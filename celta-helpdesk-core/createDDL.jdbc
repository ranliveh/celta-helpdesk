CREATE TABLE "Estado" ("EstadoPK" INTEGER NOT NULL UNIQUE, "Sigla" VARCHAR(2) NOT NULL, "Nome" VARCHAR(30) NOT NULL, PRIMARY KEY ("EstadoPK"))

CREATE TABLE "Municipio" ("MunipioPK" INTEGER NOT NULL UNIQUE, "Nome" VARCHAR(30) NOT NULL, PRIMARY KEY ("MunipioPK"))

CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
